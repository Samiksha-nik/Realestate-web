# ─── SMTP (Nodemailer) — copy to `.env` in realestate-web/ ───
# Gmail: use an App Password → https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password_here


# Where new form submissions are delivered
OWNER_EMAIL=your_email@gmail.com
MAIL_FROM_NAME=Ananya Realty Advisory LLP

# Mail API server (started with `npm run dev`)
PORT=3001

# Optional: override API URL in production (default uses Vite proxy /api → :3001)
# VITE_API_URL=https://your-domain.com
